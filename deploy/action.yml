name: 'Base Deploy'
description: 'Deploy to Norce Base Platform'
author: 'NorceTech'

branding:
  icon: 'upload-cloud'
  color: 'blue'

inputs:
  environment:
    description: 'Target environment (dev, test, stage, prod, or pr-*). Must match exactly.'
    required: true
  image_tag:
    description: 'Image tag to deploy'
    required: true
  app:
    description: 'App name (defaults to repository name)'
    required: false
  image:
    description: 'Container image name (defaults to app name). Use when the image name differs from the app name.'
    required: false
  config_file:
    description: 'Path to config file'
    required: false
    default: '.base/config.yaml'
  nginx_config_file:
    description: 'Path to custom NGINX config file (proxy buffers, headers, etc.)'
    required: false
    default: '.base/nginx.yaml'
  redirects_file:
    description: 'Path to redirects file (.yaml or .csv) for bulk redirects. Handles six-figure lists; see docs/nginx.md for the exact ceiling.'
    required: false
    default: '.base/redirects.yaml'
  api_url:
    description: 'Base Platform API URL'
    required: false
    default: 'https://base-api.norce.tech'
  api_key:
    description: 'Base Platform API key (identifies partner)'
    required: true
  wait_for_healthy:
    description: 'Wait for deployment to become healthy (true/false)'
    required: false
    default: 'true'
  wait_timeout:
    description: >
      Timeout in seconds when waiting for healthy status. Must exceed the time ArgoCD
      needs to reconcile the new tag onto the partner cluster — measured at 6-11 minutes
      on the shared controller, so 300s used to expire before the deploy had a chance.
    required: false
    default: '900'
  auto_promote:
    description: >
      Deployment strategy. Set to "false" for staged deployment (canary + manual promotion).
      Set to "true" for auto-promote (instant rollout). Omit to use the current portal setting.
    required: false
  force_https:
    description: >
      Redirect plain HTTP to HTTPS for this environment. Must be "true" or
      "false" when set; anything else fails the deploy rather than being ignored.
      Precedence is: this input, then force_https in .base/config.yaml, then the
      current portal setting. So omitting it does NOT mean "leave things alone"
      if config.yaml sets force_https — that value still applies. Omit it here
      and from config.yaml to keep whatever the portal already has.
      Note: leave this off if the domain sits behind Cloudflare in "Flexible" SSL
      mode — Cloudflare fetches the origin over HTTP, so the redirect would loop.
    required: false

outputs:
  success:
    description: 'Whether the deployment was successful'
    value: ${{ steps.final-status.outputs.success }}
  namespace:
    description: 'Kubernetes namespace'
    value: ${{ steps.deploy.outputs.namespace }}
  git_commit_sha:
    description: 'Git commit SHA for the deployment'
    value: ${{ steps.deploy.outputs.git_commit_sha }}
  previous_image_tag:
    description: 'Previous image tag'
    value: ${{ steps.deploy.outputs.previous_image_tag }}
  message:
    description: 'Deployment message'
    value: ${{ steps.final-status.outputs.message }}
  health_status:
    description: 'Final health status (Healthy, Progressing, Degraded, Suspended, etc.)'
    value: ${{ steps.wait-healthy.outputs.health_status }}
  sync_status:
    description: 'Final sync status (Synced, OutOfSync, etc.)'
    value: ${{ steps.wait-healthy.outputs.sync_status }}
  preview_url:
    description: 'Preview URL when blue-green is enabled (Suspended state). Empty for standard deployments.'
    value: ${{ steps.wait-healthy.outputs.preview_url || steps.deploy.outputs.preview_url }}
  image_digest:
    description: 'Digest returned by the platform for the submitted image. Empty when the API does not provide one.'
    value: ${{ steps.deploy.outputs.image_digest }}
  image_changed:
    description: '"true" for different previous/submitted digests, "false" for identical digests, "unknown" if either is missing. Compares image content, not rollout completion.'
    value: ${{ steps.deploy.outputs.image_changed }}

runs:
  using: 'composite'
  steps:
    - name: Deploy to Base Platform
      id: deploy
      shell: bash
      env:
        API_URL: ${{ inputs.api_url }}
        API_KEY: ${{ inputs.api_key }}
        APP: ${{ inputs.app || github.event.repository.name }}
        ENVIRONMENT: ${{ inputs.environment }}
        IMAGE_TAG: ${{ inputs.image_tag }}
        IMAGE: ${{ inputs.image }}
        COMMIT_SHA: ${{ github.sha }}
        CONFIG_FILE: ${{ inputs.config_file }}
        NGINX_CONFIG_FILE: ${{ inputs.nginx_config_file }}
        REDIRECTS_FILE: ${{ inputs.redirects_file }}
        REPO_NAME: ${{ github.event.repository.name }}
        AUTO_PROMOTE: ${{ inputs.auto_promote }}
        FORCE_HTTPS_INPUT: ${{ inputs.force_https }}
      run: bash ${{ github.action_path }}/deploy.sh

    - name: Wait for healthy deployment
      id: wait-healthy
      if: ${{ inputs.wait_for_healthy == 'true' && steps.deploy.outputs.deploy_success == 'true' }}
      shell: bash
      env:
        API_URL: ${{ inputs.api_url }}
        API_KEY: ${{ inputs.api_key }}
        APP: ${{ inputs.app || github.event.repository.name }}
        ENVIRONMENT: ${{ inputs.environment }}
        IMAGE_TAG: ${{ inputs.image_tag }}
        TIMEOUT: ${{ inputs.wait_timeout }}
        NAMESPACE: ${{ steps.deploy.outputs.namespace }}
        DEPLOY_PREVIEW_URL: ${{ steps.deploy.outputs.preview_url }}
      run: bash ${{ github.action_path }}/wait-healthy.sh

    - name: Set final status
      id: final-status
      if: always()
      shell: bash
      env:
        DEPLOY_SUCCESS: ${{ steps.deploy.outputs.deploy_success }}
        WAIT_ENABLED: ${{ inputs.wait_for_healthy }}
        HEALTHY: ${{ steps.wait-healthy.outputs.healthy }}
        HEALTH_STATUS: ${{ steps.wait-healthy.outputs.health_status }}
        DEPLOY_MESSAGE: ${{ steps.deploy.outputs.message }}
      run: bash ${{ github.action_path }}/final-status.sh
